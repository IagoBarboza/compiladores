shellSort(nums){
  int gap = 1#
  int n = length(nums)#
  while(gap < n) {
    gap = gap * 3 + 1#
  }
  gap = h / 3#
  while (gap > 0){
    repeater(int i, range(gap, n)) {
      int c = nums[i]#
      int j = i#
      while (j >= gap && nums[j - gap] > c) {
        nums[j] = nums[j - gap]#
        j = j - gap#
      }
      nums[j] = c#
    }
    gap = gap / 2#
  }
